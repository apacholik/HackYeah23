export type AnimalCoordsInfo = {
  point: [number, number];
  r: number;
};

export type SearchBoundingBox = {
  minLatitude: number,
  maxLatitude: number,
  minLongitude: number,
  maxLongitude: number,
}
