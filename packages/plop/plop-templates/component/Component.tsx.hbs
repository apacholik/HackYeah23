import { ReactNode } from "react";
import type { VariantProps } from "tailwind-variants";

import * as styles from "./{{pascalCase name}}.styled";

type Props = {
  children?: ReactNode;
} & VariantProps<typeof styles.{{camelCase name}}>;

/** Documentation for {{pascalCase name}} component */
export function {{pascalCase name}}({ children, ...restProps }: Props) {
  return (
    <div className={styles.{{camelCase name}}(restProps)}>
      <span>{{pascalCase name}} works!</span>
      {children}
    </div>
  );
}
