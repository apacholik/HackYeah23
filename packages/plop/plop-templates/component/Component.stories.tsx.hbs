import type { ComponentMeta, ComponentStory } from "@storybook/react";
import { decorators, parameters } from "ui/stories/common";

import { {{pascalCase name}} } from "./{{pascalCase name}}";

export default {
  title: "{{pascalCase namespace}} Components/{{pascalCase name}}",
  component: {{pascalCase name}},
  decorators: [...decorators],
  parameters: { ...parameters },
} as ComponentMeta<typeof {{pascalCase name}}>;

const Template: ComponentStory<typeof {{pascalCase name}}> = (props) => (
  <{{pascalCase name}} {...props} />
);

export const Default = Template.bind({});
Default.args = {
  children: "",
};
